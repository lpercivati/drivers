package tests

/*func Test_ShouldGetDrivers(t *testing.T) {

	driverController := controllers.DriverController{
		Service: new(mocks.DriverService),
	}

	w := httptest.NewRecorder()
	context, _ := gin.CreateTestContext(w)

	//context.Params = []gin.Param{gin.Param{Key: "count", Value: "1"}, gin.Param{Key: "page", Value: "1"}}
	m := make(map[string]string)
	m["count"] = "1"
	m["page"] = "1"

	context.Request.URL.Query()

	driverController.Get(context)

	assert.Equal(t, 200, w.Result().StatusCode)
}
*/
